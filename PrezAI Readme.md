# PrezAI — Генератор презентаций на основе LLM

**PrezAI** — AI-powered генератор презентаций, который автоматически создаёт PPTX-файлы по заданной теме. Система использует мультиагентный подход: LLM анализирует шаблон, планирует структуру, генерирует контент и собирает готовую презентацию — всё автоматически.

## Возможности

- 🤖 **Мультиагентная генерация** — PlannerAgent планирует структуру, WriterAgent генерирует контент каждого слайда параллельно
- 📐 **Динамический анализ шаблонов** — автоматическое извлечение структуры из любого PPTX-шаблона (с `{{плейсхолдерами}}` и без них)
- 🎨 **Умный выбор шаблона** — LLM выбирает оптимальный шаблон из каталога Google Sheets по теме, стилю и оформлению
- 📝 **Mixed-content слайды** — поддержка текста, маркированных и нумерованных списков в одном слайде
- 🌐 **Веб-интерфейс и API** — FastAPI-сервер с чат-интерфейсом для генерации презентаций через браузер
- ☁️ **Интеграция с Google Drive** — автоматическое скачивание шаблонов по публичной ссылке

## Архитектура

```
Тема → TemplateSelector → Скачивание шаблона → TemplateParser → PlannerAgent → WriterAgent (×N) → PPTX Builder → result.pptx
```

### Пайплайн генерации

1. **TemplateSelector** (`template_selector.py`) — загружает каталог шаблонов из Google Sheets, отправляет в LLM для выбора лучшего варианта по теме/стилю/оформлению
2. **Скачивание шаблона** (`google_drive.py`) — скачивает выбранный шаблон с Google Drive; при недоступности использует локальный fallback
3. **TemplateParser** (`template_parser.py`) — парсит PPTX на уровне XML, извлекает `{{плейсхолдеры}}`; для шаблонов без маркеров вызывает LLM для анализа
4. **PlannerAgent** (`agent_system.py`) — принимает тему и доступные типы слайдов, планирует оптимальную структуру презентации (5–9 слайдов)
5. **WriterAgent** (`agent_system.py`) — параллельно генерирует контент для каждого слайда с учётом общего плана
6. **PPTX Builder** (`generation_pres.py`) — собирает итоговый PPTX: дублирует слайды, подставляет контент, перестраивает XML-структуру файла

## Структура проекта

```
├── main.py                  # CLI точка входа
├── api.py                   # FastAPI-сервер с веб-интерфейсом
├── agent_system.py          # Мультиагентная система (Planner + Writer)
├── template_selector.py     # Выбор шаблона через LLM + Google Sheets
├── template_parser.py       # Динамический анализ структуры PPTX
├── generation_pres.py       # Сборка итогового PPTX из шаблона + контента
├── extract_structure_pres.py# Утилита извлечения плейсхолдеров из PPTX
├── google_drive.py          # Скачивание шаблонов с Google Drive
├── index.html               # Веб-интерфейс (чат с AI)
├── prompts/                 # Промпты для LLM-агентов
│   ├── planner_system.txt        # Системный промпт планировщика
│   ├── planner_user.txt          # Пользовательский промпт планировщика
│   ├── writer_system.txt         # Системный промпт автора слайдов
│   ├── writer_user.txt           # Пользовательский промпт автора
│   ├── writer_list_format_hint.txt  # Формат списков для автора
│   ├── template_analyzer_system.txt # Анализатор шаблонов (системный)
│   ├── template_analyzer_user.txt   # Анализатор шаблонов (пользовательский)
│   ├── template_selector_system.txt # Выбор шаблона (системный)
│   └── template_selector_user.txt   # Выбор шаблона (пользовательский)
├── .env                     # Конфигурация (LLM, Google Sheets, fallback)
├── structure.json           # Пример структуры шаблона
├── content.json             # Пример сгенерированного контента
├── template.pptx            # Скачанный шаблон
├── test.pptx                # Локальный fallback-шаблон
└── result.pptx              # Пример результата
```

## Установка

### Зависимости

```bash
pip install openai httpx python-dotenv lxml python-pptx pandas requests fastapi uvicorn pydantic
```

### Конфигурация

Создайте файл `.env` в корне проекта:

```env
# LLM настройки (OpenAI-совместимый API)
LLM_BASE_URL=http://your-llm-server:8000/v1
LLM_API_KEY=your-api-key
LLM_MODEL=your-model-name
LLM_TIMEOUT=600.0

# Google Sheets с каталогом шаблонов
TEMPLATES_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit

# Fallback-шаблон (Google Drive ссылка)
FALLBACK_TEMPLATE_URL=https://docs.google.com/presentation/d/YOUR_PRESENTATION_ID/edit

# Локальный fallback если Google Drive недоступен
FALLBACK_LOCAL_TEMPLATE=test.pptx
```

LLM-сервер должен поддерживать OpenAI-совместимый API (`/v1/chat/completions`).

## Использование

### CLI

```bash
# Тема по умолчанию
python main.py

# Своя тема
python main.py "Будущее искусственного интеллекта в образовании"

# С указанием стиля и оформления
python main.py "Квартальный отчёт продаж" --style minimalism --theme dark

# С дополнительным контекстом
python main.py "Стратегия роста" --prompt "деловая аудитория, строгий стиль"
```

Результат сохраняется в `result.pptx`.

### API-сервер

```bash
# Запуск сервера
python api.py

# Или через uvicorn
uvicorn api:app --host 0.0.0.0 --port 8080 --reload
```

#### Эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/` | Веб-интерфейс (чат) |
| `GET` | `/health` | Проверка состояния |
| `POST` | `/api/chat` | Генерация презентации |

#### Пример запроса

```bash
curl -X POST http://localhost:8080/api/chat \
  -H "Content-Type: application/json" \
  -d '{"text": "Тренды машинного обучения 2025", "style": "minimalism", "theme": "light", "slides": 10}' \
  --output result.pptx
```

Ответ — PPTX-файл в бинарном формате.

### Веб-интерфейс

Откройте `http://localhost:8080` в браузере. Интерфейс предоставляет чат с AI для генерации презентаций — введите тему, выберите параметры и скачайте готовый PPTX.

## Как это работает

### Формат шаблонов

Шаблоны PPTX содержат плейсхолдеры вида `{{TITLE_TITLE}}`, `{{BULLET_1}}`, `{{ITEMS}}` и т.д. TemplateParser извлекает их из XML и строит динамическую структуру. Для шаблонов без маркеров LLM самостоятельно определяет, какие текстовые блоки являются заглушками.

### Формат контента

Сгенерированный контент (`content.json`) содержит массив слайдов с типом и заполненными полями:

```json
{
  "slides": [
    {
      "slide_type": "TITLE",
      "replacements": {
        "{{TITLE_TITLE}}": "Будущее ИИ в образовании",
        "{{SUBTITLE}}": "Тренды, вызовы и возможности"
      }
    },
    {
      "slide_type": "BULLETS_4",
      "replacements": {
        "{{TITLE_BULLETS_4}}": "Ключевые направления",
        "{{ITEMS}}": [
          {"type": "bullet", "value": "Персонализация обучения"},
          {"type": "bullet", "value": "Автоматическая оценка знаний"},
          {"type": "bullet", "value": "Генерация учебных материалов"}
        ]
      }
    }
  ]
}
```

Списковые поля поддерживают три типа элементов: `text` (обычный текст), `bullet` (маркированный) и `numbered` (нумерованный).

## Технологии

- **Python 3.11+**
- **OpenAI SDK** — для работы с LLM через OpenAI-совместимый API
- **lxml** — парсинг и модификация XML внутри PPTX
- **python-pptx** — вспомогательная работа с презентациями
- **FastAPI + Uvicorn** — HTTP API и веб-сервер
- **pandas** — обработка таблицы шаблонов из Google Sheets
- **httpx** — HTTP-клиент с настраиваемыми таймаутами

## Лицензия

Проект разработан в рамках хакатона **Axenix AI**.